import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    profilePicture : ""
}

export const profileSlice = createSlice({
    name:'profile',
    initialState,
    reducers:{

    }
})
